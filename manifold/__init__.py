#!/usr/bin/env python3

from manifold.call import Call as Call
from manifold.log import setup_logger
from manifold.multicall import MultiCall as MultiCall

# by default perform no logs
setup_logger("CRITICAL")
